<Window x:Class="MiniMes.Client.Views.WorkOrderEditView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        Title="작업 지시 등록/수정" Height="250" Width="350"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Themes/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <Style TargetType="{x:Type TextBox}" BasedOn="{StaticResource MetroTextBoxStyle}">
                <Setter Property="FontSize" Value="11"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
            </Style>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="0" Text="품목 코드:" Margin="5" VerticalAlignment="Center"/>
        <TextBox Grid.Row="0" Grid.Column="1" Margin="5" Text="{Binding ItemCode, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="{Binding IsItemCodeReadOnly}"/>
        <TextBlock Grid.Row="1" Grid.Column="0" Text="지시 수량:" Margin="5" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1" Margin="5" Text="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBlock Grid.Row="2" Grid.Column="0" Text="지시 ID:" Margin="5" VerticalAlignment="Center"/>
        <TextBox Grid.Row="2" Grid.Column="1" Margin="5" Text="{Binding Id, Mode=OneWay}" IsReadOnly="True"/>
        <StackPanel Grid.Row="6" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button
                  FontSize="11"
                  Margin="5" 
                  Height="30"              
                  MinWidth="70" 
                  Cursor="Hand"
                  Style="{StaticResource MetroBlueButtonStyle}"
                  Content="저장" 
                  Click="SaveButton_Click" />
            <Button 
                FontSize="11"
                Margin="5" 
                Height="30"              
                MinWidth="70" 
                Cursor="Hand"
                Style="{StaticResource MetroPinkButtonStyle}"
                Content="취소" 
                Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>