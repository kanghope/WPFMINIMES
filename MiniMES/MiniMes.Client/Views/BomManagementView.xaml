<UserControl x:Class="MiniMes.Client.Views.BomManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:MiniMes.Client.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=vm:BomManagementViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="700" d:DesignWidth="1100" Background="White">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Themes/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <BooleanToVisibilityConverter x:Key="BoolToVis"/>
            <Style x:Key="ItemTypeStyle" TargetType="TextBlock">
                <Setter Property="HorizontalAlignment" Value="Center"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="FontWeight" Value="Bold"/>
               
            </Style>
            <Style TargetType="{x:Type TextBox}" BasedOn="{StaticResource MetroTextBoxStyle}">
                <Setter Property="FontSize" Value="11"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="150"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="15,10" Background="#F8F9FA" BorderBrush="#CCCCCC" BorderThickness="0,0,0,1">
            <Grid>
                <TextBlock Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" FontSize="18" FontWeight="Bold" VerticalAlignment="Center" Foreground="#2C3E50"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock Text="완제품 검색 :" VerticalAlignment="Center" Margin="10,0,5,0"/>
                    <TextBox Width="150" Height="25" Tag="완제품 코드/명 입력..."/>

                    <Button Content="🔍 완제품 조회" 
                            Command="{Binding LoadParentsCommand}" 
                            Margin="10,0,0,0" Padding="12,0" Height="28"
                            Style="{StaticResource MetroBlueButtonStyle}"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Background="#F0F0F0" BorderBrush="#CCCCCC" BorderThickness="0,0,0,1" Padding="15,7">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="📊 선택된 부모 정보:" FontWeight="Bold" Foreground="#333333" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding SelectedParent.ItemName, TargetNullValue='선택되지 않음'}" Margin="10,0,5,0" VerticalAlignment="Center" Foreground="#0078D7" FontWeight="SemiBold"/>
                <TextBlock Text="{Binding SelectedParent.ItemCode, StringFormat='({0})'}" VerticalAlignment="Center" Foreground="#666666"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="2" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.5*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <GroupBox Grid.Column="0" Header="완제품 목록 (Parent Items)" BorderBrush="#CCCCCC">
                <DataGrid Style="{StaticResource MetroDataGridStyle}"
                          ItemsSource="{Binding ParentItems}" 
                          SelectedItem="{Binding SelectedParent}"
                          AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="품목 코드" Binding="{Binding ItemCode}" Width="100" >
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource CenterTextColumnStyle}"/>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="완제품 명칭" Binding="{Binding ItemName}" Width="*">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource CenterTextColumnStyle}"/>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                       
                        <DataGridTextColumn Header="단위" Binding="{Binding ItemUnit}" Width="60" ElementStyle="{StaticResource CenterTextColumnStyle}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>

            <GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Stretch" Background="Transparent" Margin="2,0"/>

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,10">
                    <Button Content="➕ 자재 추가" Command="{Binding AddDetailCommand}" Margin="5,0" Width="90" Style="{StaticResource MetroBlueButtonStyle}"/>
                    <Button Content="💾 BOM 저장" Command="{Binding SaveDetailCommand}" Margin="5,0" Width="90" Style="{StaticResource MetroBlueButtonStyle}"/>
                    <Button Content="🗑 삭제" Command="{Binding DeleteDetailCommand}" Margin="5,0" Width="90" Style="{StaticResource MetroBlueButtonStyle}"/>
                </StackPanel>

                <GroupBox Grid.Row="1" Header="BOM 상세 구성 (Child Items)" BorderBrush="#CCCCCC">
                    <DataGrid Style="{StaticResource MetroDataGridStyle}"
                              ItemsSource="{Binding BomDetails}" 
                              SelectedItem="{Binding SelectedBom}"
                              AutoGenerateColumns="False" CanUserAddRows="False">
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="자재 코드" Width="140">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding ChildItemCode}" VerticalAlignment="Center" Margin="5,0"/>
                                            <Button Grid.Column="1" Content="🔍" 
                                            Command="{Binding DataContext.OpenItemPopupCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}" 
                                            Style="{StaticResource MetroBlueButtonStyle}" Width="25" Height="22" Margin="2"/>
                                        </Grid>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="자재명 (품명)" Binding="{Binding ChildItemName}" Width="*" IsReadOnly="True"/>
                            <DataGridTextColumn Header="규격" Binding="{Binding ChildItemSpec}" Width="100" IsReadOnly="True"/>
                            <DataGridTextColumn Header="단위" Binding="{Binding ChildItemUnit}" Width="50" IsReadOnly="True" ElementStyle="{StaticResource CenterTextColumnStyle}"/>
                            <DataGridTextColumn Header="소요량" Binding="{Binding Consumption, StringFormat={}{0:N2}}" Width="80" FontWeight="Bold">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="Margin" Value="0,0,10,0"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </GroupBox>
            </Grid>
        </Grid>

        <GroupBox Grid.Row="3" Header="📜 시스템 처리 로그" Margin="10,0,10,10" BorderBrush="#CCCCCC">
            <ListBox Background="#1E1E1E" Foreground="#ADFF2F" 
                     FontSize="11" FontFamily="Consolas" BorderThickness="0"
                     ItemsSource="{Binding CommunicationLogs}"/>
        </GroupBox>

        <Grid Grid.Row="0" Grid.RowSpan="4" Background="#AAFFFFFF" 
              Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}">
            <Border Background="White" BorderBrush="#0078D7" BorderThickness="1" 
                    VerticalAlignment="Center" HorizontalAlignment="Center" 
                    Padding="40,30" CornerRadius="8">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="15" Direction="270" Opacity="0.2" ShadowDepth="3"/>
                </Border.Effect>

                <StackPanel Width="300">
                    <TextBlock Text="BOM 데이터를 불러오는 중입니다" FontSize="16" FontWeight="Bold" 
                               Foreground="#333333" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                    <ProgressBar Value="{Binding LoadingProgress}" 
                                 Minimum="0" Maximum="100" 
                                 Height="12" Foreground="#0078D7" Background="#EEEEEE"/>

                    <TextBlock Text="{Binding LoadingProgress, StringFormat={}{0:F0}%}" 
                               HorizontalAlignment="Right" Foreground="#0078D7" 
                               FontSize="12" FontWeight="Bold" Margin="0,5,0,0"/>

                    <TextBlock Text="잠시만 기다려 주십시오..." 
                               Margin="0,20,0,0" HorizontalAlignment="Center" 
                               FontSize="11" Foreground="#666666"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>