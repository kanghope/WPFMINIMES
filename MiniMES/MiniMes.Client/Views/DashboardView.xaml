<UserControl x:Class="MiniMes.Client.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Themes/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>

   
            <Style TargetType="{x:Type TextBox}" BasedOn="{StaticResource MetroTextBoxStyle}">
                <Setter Property="FontSize" Value="11"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
            </Style>
        </ResourceDictionary>

    </UserControl.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="실시간 생산 현황 대시보드" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <UniformGrid Grid.Row="1" Columns="2" Rows="2">
            <Border Background="#E3F2FD" Margin="10" CornerRadius="10" BorderBrush="#2196F3" BorderThickness="1">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="오늘의 양품" FontSize="16" Foreground="#1976D2"/>
                    <TextBlock Text="{Binding TotalGood, StringFormat={}{0:N0}}" FontSize="40" FontWeight="Bold" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>

            <Border Background="#FFEBEE" Margin="10" CornerRadius="10" BorderBrush="#F44336" BorderThickness="1">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="오늘의 불량" FontSize="16" Foreground="#D32F2F"/>
                    <TextBlock Text="{Binding TotalBad, StringFormat={}{0:N0}}" FontSize="40" FontWeight="Bold" HorizontalAlignment="Center" Foreground="#D32F2F"/>
                </StackPanel>
            </Border>

            <Border Background="#E8F5E9" Margin="10" CornerRadius="10" BorderBrush="#4CAF50" BorderThickness="1">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="생산 달성률" FontSize="16" Foreground="#388E3C"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="{Binding AchievementRate}" FontSize="40" FontWeight="Bold"/>
                        <TextBlock Text="%" FontSize="20" VerticalAlignment="Bottom" Margin="5,0,0,5"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border Background="#FFF3E0" Margin="10" CornerRadius="10" BorderBrush="#FF9800" BorderThickness="1">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="진행 중인 작업" FontSize="16" Foreground="#F57C00"/>
                    <TextBlock Text="{Binding ActiveOrderCount}" FontSize="40" FontWeight="Bold" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </UniformGrid>
    </Grid>
</UserControl>